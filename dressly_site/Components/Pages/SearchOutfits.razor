@page "/search-outfit"

<PageTitle>חיפוש אאוטפיט</PageTitle>

<h3>חפש אאוטפיטים</h3>

<EditForm Model="@searchCriteria" OnValidSubmit="HandleSearch">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div>
        <label>קטגוריה:</label>
        <InputText @bind-Value="searchCriteria.Category" />
    </div>

    <div>
        <label>עונה:</label>
        <InputText @bind-Value="searchCriteria.Season" />
    </div>

    <button type="submit">חפש</button>
</EditForm>

<div>
    <h4>תוצאות חיפוש</h4>
    <!-- הצג תוצאות חיפוש כאן -->
</div>

@code {
    private SearchCriteria searchCriteria = new SearchCriteria();

    private void HandleSearch()
    {
        // הוסף לוגיקה לחיפוש אאוטפיטים
    }

    public class SearchCriteria
    {
        public string Category { get; set; }
        public string Season { get; set; }
    }
}
    